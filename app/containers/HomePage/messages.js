import { defineMessages } from 'react-intl';

export const scope = 'app.containers.homepage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the default HomePage container!',
  },
});
