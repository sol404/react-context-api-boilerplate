import { defineMessages } from 'react-intl';

export const scope = 'app.containers.contactpage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the default ContactPage container!',
  },
});
